#define _USES_MATH_DEFINES

#include <iostream>
#include <math.h>

using namespace std;

int main(void)
{
	double result;
	double x, y, z;
	double a, b, c, d, e;
	double phi_x;
	
	cout << "Please enter X, Y, Z: ";
	cin >> x >> y >> z;

	return 0;
}